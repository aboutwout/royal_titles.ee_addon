It supports the following global variables:

    {site_id}
    {weblog_id}
    {blog_name}
    {blog_title}
    {blog_lang}
    {current_time format='%m %d %Y'}
    // and a shitload more, but those are useless...